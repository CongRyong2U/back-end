const express = require("express");
const router = express.Router();
const { Illust } = require("../models/Illust");

router.get("/list", (req, res) => {

  
});

router.get("/detail/:illustId", (req, res) => {

  
});

module.exports = router;
